name: PR Policy

on:
  pull_request:
    types: [opened, edited, synchronize, reopened]

permissions:
  contents: read

jobs:
  title:
    name: PR title
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - name: Validate PR title
        env:
          TITLE: ${{ github.event.pull_request.title }}
        run: |
          if [[ ! "$TITLE" =~ ^(feat|fix|docs|content|refactor|chore|ci|perf|build|revert)(\(.+\))?:\ .+ ]]; then
            echo "Use a clear title such as content: update project evidence or fix: correct mobile header layout"
            exit 1
          fi
